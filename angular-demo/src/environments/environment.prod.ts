/**
 * Production environment. Substituted in at build time via angular.json's
 * `configurations.production.fileReplacements` — see angular.json.
 *
 * Mirrors environment.ts structurally so consumers can use a single
 * import regardless of build mode.
 */
export const environment = {
  production: true,
  recorderEnabled: false,
};
